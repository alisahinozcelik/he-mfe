import { ShowSepetPipe } from './show-sepet.pipe';

describe('ShowSepetPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowSepetPipe();
    expect(pipe).toBeTruthy();
  });
});
