import { FilterListePipe } from './filter-liste.pipe';

describe('FilterListePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListePipe();
    expect(pipe).toBeTruthy();
  });
});
